/*

Hydration Errors

"Hydration mismatch makes React throw away server HTML and re-render 
from scratch on the client — killing SSR's benefits. 
Fix it by isolating dynamic values to client components with useEffect,
or use ssr: false for browser-only code."

use this on element suppressHydrationWarning
*/